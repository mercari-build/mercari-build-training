CREATE TABLE IF NOT EXISTS items(
        id INTEGER PRIMARY KEY,
        name TEXT,
        category_id INTEGER,
        image_name TEXT
);

CREATE TABLE IF NOT EXISTS category(
        id INTEGER PRIMARY KEY,
        name TEXT
);