CREATE TABLE IF NOT EXISTS "items"(id integer NOT NULL PRIMARY KEY, name text, category_id integer, image_name string);
CREATE TABLE IF NOT EXISTS "categories"(id integer NOT NULL PRIMARY KEY, name text UNIQUE);
